package exercises;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Scanner;

public class ArrayListPracticeWithWordsChapter2 {
    public static void main(String[] arg){
        Scanner input = new Scanner(System.in);
        String strr = "I would not, could not, in a box. I would not, could not with a fox. I will not eat them in a house. I will not eat them with a mouse.";
      /*  ArrayList <String> str = new ArrayList<>();
        strr = strr.split();

        System.out.println(Arrays.toString(strr));
        String index = "0";
        /*System.out.println("Enter the desired length of string");
        index = input.nextLine();
        int count = 0;
        count = Integer.parseInt(index);
        System.out.println("Enter the words (or Press Enter)");
       // int index = 0;
        String newStr;
        do{
            newStr = input.nextLine();
            if(!newStr.equals("") && newStr.length()==count) {
                str.add(newStr);
            }
        }while(!newStr.equals(""));

        System.out.println(str); */

    }
}
